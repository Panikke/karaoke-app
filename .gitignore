node_modules/
dist/
.pnpm-store/
server/node_modules/

# Environment — contains Supabase keys, never commit
.env
.env.*

# Safe to commit — example template only
!.env.example
# Safe to commit — only the public server URL, no secrets
!.env.capacitor

# Capacitor / Android build artifacts
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/
*.apk
*.aab

# OS
.DS_Store
Thumbs.db
.claude/
